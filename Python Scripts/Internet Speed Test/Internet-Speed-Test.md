# Internet Speed Test

Run a one-time Speedtest.net bandwidth check without installing a package globally:

```powershell
uvx speedtest-cli
```

The command reports download speed, upload speed, and ping. It transfers network data as part of the measurement.
