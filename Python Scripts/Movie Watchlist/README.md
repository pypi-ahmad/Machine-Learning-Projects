# Movie Watchlist

A local Streamlit app for tracking movies, watch status, ratings, reviews, and simple watch statistics.

```powershell
uv sync --no-config
uv run --no-config streamlit run main.py
```

Watchlist data is stored in `movies.json` beside `main.py`.
