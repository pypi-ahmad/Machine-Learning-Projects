[project]
name = "folder-splitter"
version = "0.1.0"
description = "Preview and safely copy direct folder files into numbered groups"
readme = "Readme.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
