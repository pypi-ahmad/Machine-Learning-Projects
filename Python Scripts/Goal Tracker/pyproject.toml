[project]
name = "goal-tracker"
version = "0.1.0"
description = "Local Streamlit goal tracker"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pandas>=2.2", "streamlit>=1.57"]

[tool.uv]
package = false
