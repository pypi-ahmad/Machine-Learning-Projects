# Memory Card Game

A standard-library terminal memory-match game.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

The game runs locally and does not access the network or write persistent data.
