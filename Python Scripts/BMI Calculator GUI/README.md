# BMI Calculator GUI

Calculate BMI with metric or imperial height and weight inputs in a Tkinter
desktop window.

```powershell
cd "Python Scripts/BMI Calculator GUI"
uv sync
uv run python main.py
```

BMI is a screening measure, not a diagnosis. Use results with clinical context
and seek qualified medical advice when needed.
