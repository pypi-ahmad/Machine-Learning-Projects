[project]
name = "flashcard-app"
version = "0.1.0"
description = "Local Streamlit flashcard study app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.57",
]

[tool.uv]
package = false
