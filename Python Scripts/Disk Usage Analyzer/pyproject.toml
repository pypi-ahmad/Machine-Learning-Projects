[project]
name = "disk-usage-analyzer"
version = "0.1.0"
description = "Read-only CLI for analyzing directory and drive usage"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
