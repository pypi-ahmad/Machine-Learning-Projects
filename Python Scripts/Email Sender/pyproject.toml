[project]
name = "email-sender"
version = "0.1.0"
description = "Preview-first personalized Gmail SMTP batch sender"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
