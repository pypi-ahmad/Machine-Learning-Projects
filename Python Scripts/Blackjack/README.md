# Blackjack

Play a terminal Blackjack game against the dealer. The game supports hit, stand,
and double-down; split is intentionally not offered because a complete second
hand is not implemented.

```powershell
cd "Python Scripts/Blackjack"
uv sync
uv run python main.py
```

The game uses only the Python standard library.
