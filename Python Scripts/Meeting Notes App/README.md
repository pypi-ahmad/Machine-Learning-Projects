# Meeting Notes App

A local Streamlit app for recording, browsing, searching, and exporting meeting notes, action items, and decisions.

```powershell
uv sync --no-config
uv run --no-config streamlit run main.py
```

Notes are stored in `meetings.json` beside `main.py`. Keep a backup of that file when moving or deleting the project.
