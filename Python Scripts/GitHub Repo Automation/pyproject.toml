[project]
name = "github-repo-automation"
version = "0.1.0"
description = "Preview and explicitly create a GitHub repository and local folder"
readme = "ReadMe.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
