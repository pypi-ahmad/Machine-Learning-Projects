# Bill Splitter

Track shared expenses, balances, and suggested settlements in a local Streamlit
dashboard.

```powershell
cd "Python Scripts/Bill Splitter"
uv sync
uv run streamlit run main.py
```

Data is stored locally in `bills.json`, which is excluded from Git. The app is
for informal planning; review settlements before transferring money.
