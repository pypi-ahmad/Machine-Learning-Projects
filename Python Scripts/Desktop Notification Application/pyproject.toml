[project]
name = "desktop-notification-application"
version = "0.1.0"
description = "Configurable desktop break reminder"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "plyer>=2.1.0",
]

[tool.uv]
package = false
