[project]
name = "daily-journal"
version = "0.1.0"
description = "Local Streamlit journal with mood tags and CSV export"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.3.3",
    "streamlit>=1.57.0",
]

[tool.uv]
package = false
