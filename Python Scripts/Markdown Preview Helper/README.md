# Markdown Preview Helper

A standard-library CLI for previewing Markdown as formatted terminal text.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

It reads Markdown locally and does not modify source files unless an explicit write option is used.
