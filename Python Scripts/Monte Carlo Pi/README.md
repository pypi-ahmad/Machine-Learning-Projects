# Monte Carlo Pi

A standard-library CLI for estimating pi by sampling random points inside a square.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

The simulation runs locally. Use a fixed seed when reproducible output is needed.
