# GPA Calculator

Interactive weighted GPA calculator using a 4.0 grade scale.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

Credits and weights must be finite positive values. The grade scale is illustrative; confirm how your institution calculates GPA before relying on the result. Requires Python 3.13 or later and no third-party packages.
