[project]
name = "form-builder"
version = "0.1.0"
description = "Local Tkinter form builder and response exporter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
