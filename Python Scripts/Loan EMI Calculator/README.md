# Loan EMI Calculator

A local command-line calculator for monthly EMI, total interest, repayment totals, amortization schedules, and side-by-side loan comparisons.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

Results are mathematical estimates only, not a loan offer or financial advice.
