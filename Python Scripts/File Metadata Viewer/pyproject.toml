[project]
name = "file-metadata-viewer"
version = "0.1.0"
description = "Read-only filesystem, image, and PDF metadata viewer"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=11",
    "pypdf>=5",
]

[tool.uv]
package = false
