[project]
name = "current-weather-fetcher"
version = "0.1.0"
description = "Interactive current-weather lookup using the OpenWeather API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32.5",
]

[tool.uv]
package = false
