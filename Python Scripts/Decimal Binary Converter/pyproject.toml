[project]
name = "decimal-binary-converter"
version = "0.1.0"
description = "Interactive decimal and binary integer converter"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
