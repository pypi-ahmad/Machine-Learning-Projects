[project]
name = "equation-solver"
version = "0.1.0"
description = "Command-line solver for basic linear equations and systems"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
