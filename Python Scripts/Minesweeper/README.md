# Minesweeper

A standard-library terminal implementation of Minesweeper.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The game runs locally and does not access the network or write persistent data.
