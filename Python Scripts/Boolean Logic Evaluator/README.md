# Boolean Logic Evaluator

Evaluate boolean expressions and generate truth tables interactively.

```powershell
cd "Python Scripts/Boolean Logic Evaluator"
uv sync
uv run python main.py
```

Supported operators are `AND`, `OR`, `NOT`, and `XOR`, with symbol forms
`&&`, `||`, `!`, and `^` where applicable.
