# Markdown to HTML

A standard-library CLI for converting local Markdown files to HTML.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

Provide explicit input and output paths when converting files. The tool reads local Markdown and writes HTML only when invoked with a conversion command.
