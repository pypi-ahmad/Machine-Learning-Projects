[project]
name = "currency-exchange-rates"
version = "0.1.0"
description = "Interactive exchange-rate table viewer backed by x-rates.com"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "requests>=2.32.5",
]

[tool.uv]
package = false
