# Inventory Manager GUI

A local Tkinter inventory manager with product CRUD, category filtering, search, and low-stock alerts.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The application stores its data in `inventory.json` beside `main.py`. Back up that file before moving or deleting the project.
