# Log File Analyzer

A standard-library CLI for parsing common web-server, Python, and generic log formats; filtering entries; and reporting errors or frequency summaries.

```powershell
uv sync --no-config
uv run --no-config python main.py app.log
```

The tool reads the selected log file locally and does not modify it.
