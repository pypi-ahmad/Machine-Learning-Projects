# JSON Formatter

A standard-library CLI for validating, pretty-printing, minifying, sorting, flattening, querying, and comparing JSON.

```powershell
uv sync --no-config
uv run --no-config python main.py data.json
```

Passing a JSON file prints formatted output without changing it. The interactive menu includes an explicit save prompt for writes.
