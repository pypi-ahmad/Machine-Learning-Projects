# Matrix Calculator

A standard-library interactive CLI for matrix arithmetic, transpose, determinant, inverse, and row reduction.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The calculator runs locally and does not write files or use the network.
