[project]
name = "directory-tree-viewer"
version = "0.1.0"
description = "Interactive CLI for viewing and exporting directory trees"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
