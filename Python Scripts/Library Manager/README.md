# Library Manager

A local Tkinter app for managing books, members, loans, and due dates.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The application stores its records in `library.json` beside `main.py`. Back up that file before moving or deleting the project.
