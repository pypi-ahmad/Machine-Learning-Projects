# Invoice Tool

A local Tkinter desktop tool for creating, managing, and exporting invoices with line items and tax calculations.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The application stores invoice records and company settings in `invoices.json` beside `main.py`. Back up that file before moving or deleting the project.
