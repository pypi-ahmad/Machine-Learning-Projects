[project]
name = "document-qa"
version = "0.1.0"
description = "Local TF-IDF sentence retrieval demo built with Streamlit"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "streamlit>=1.57.0",
]

[tool.uv]
package = false
