# Loan Eligibility

A local Streamlit learning demo that applies a transparent heuristic score to synthetic applicant inputs. It is not a credit decision, loan offer, or financial advice.

```powershell
uv sync --no-config
uv run --no-config streamlit run main.py
```

The app runs locally and does not send or store submitted values.
