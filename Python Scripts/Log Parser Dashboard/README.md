# Log Parser Dashboard

A standard-library CLI for parsing and summarizing local log files.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

The tool reads selected log files locally and does not modify them.
