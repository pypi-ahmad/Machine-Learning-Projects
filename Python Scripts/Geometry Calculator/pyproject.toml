[project]
name = "geometry-calculator"
version = "0.1.0"
description = "Interactive command-line geometry calculator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
