# Maze Generator

A standard-library CLI for generating and optionally solving text mazes.

```powershell
uv sync --no-config
uv run --no-config python main.py --help
```

The generator runs locally. Use its explicit output option when saving a maze to a file.
