[project]
name = "duplicate-file-remover"
version = "0.1.0"
description = "Preview-first non-recursive duplicate file detector"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
