# Lorem Ipsum Generator

A standard-library CLI for generating classic placeholder text by word, sentence, or paragraph count.

```powershell
uv sync --no-config
uv run --no-config python main.py --words 100
```

It runs locally and does not access the network or write files unless you redirect its output.
