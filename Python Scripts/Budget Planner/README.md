# Budget Planner

Track category budgets and local expenses in a Streamlit dashboard.

```powershell
cd "Python Scripts/Budget Planner"
uv sync
uv run streamlit run main.py
```

Data is stored locally in `budget.json`, which is excluded from Git. The app is
for personal planning and does not connect to financial accounts.
