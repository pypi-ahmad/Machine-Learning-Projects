# Morse Code Translator

A standard-library CLI for encoding and decoding International Morse code.

```powershell
uv sync --no-config
uv run --no-config python main.py
```

The translator runs locally and does not access the network or persist data.
