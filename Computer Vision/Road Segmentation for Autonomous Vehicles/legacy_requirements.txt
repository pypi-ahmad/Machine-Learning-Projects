# Legacy dependencies for Road segmentation for autonomous vechicles
# This project currently uses TensorFlow/Keras (U-Net)
# These deps are isolated here pending PyTorch migration
tensorflow>=2.10.0
