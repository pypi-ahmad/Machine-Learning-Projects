# Legacy dependencies for Food Object Detection
# This project currently uses TensorFlow/Keras (InceptionV3) + Streamlit
# Original pinned: tensorflow==2.5.0
# These deps are isolated here pending PyTorch migration
tensorflow>=2.10.0
