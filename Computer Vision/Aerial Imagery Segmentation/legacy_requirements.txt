# Legacy dependencies for Aerial Imagery Segmentation
# This project currently uses TensorFlow/Keras (mixed tf.keras + standalone keras)
# These deps are isolated here pending PyTorch migration
tensorflow>=2.10.0
keras>=2.10.0
