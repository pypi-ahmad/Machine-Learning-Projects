# YOLOv5 Clone Relocated

The `yolov5-master/` directory that was previously here has been moved to:

```
/legacy/yolov5_clones/licence_plate_detector/
```

This was done as part of Phase 1A dependency sanitization.
The project code (`best.pt`, detection scripts) remains here.

**To restore** (if needed for legacy testing):
```bash
cp -r ../../legacy/yolov5_clones/licence_plate_detector ./yolov5-master
```
