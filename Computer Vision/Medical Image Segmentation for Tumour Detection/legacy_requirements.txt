# Legacy dependencies for Medical Image Segmentaion for Tumour Detection
# This project currently uses TensorFlow/Keras (U-Net) + nilearn/nibabel
# These deps are isolated here pending PyTorch migration
tensorflow>=2.10.0
nilearn>=0.10.0
nibabel>=5.0.0
