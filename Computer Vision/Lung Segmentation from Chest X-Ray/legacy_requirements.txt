# Legacy dependencies for Lung Segmentation From Chest X-Ray
# This project currently uses standalone Keras (U-Net)
# These deps are isolated here pending PyTorch migration
tensorflow>=2.10.0
keras>=2.10.0
